using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class NextLevelFadeIn : MonoBehaviour {
	/*
	public float fadeInTime = 1f;

	private Image fadePanel;
	private Color currentColor = new Color(255f, 255f, 255f, 0f); // Alpha = 0 initally

	void Start() {
		fadePanel = GetComponent<Image>();
	}

	void Update() {
		// TODO check if previous level was won (use PlayerPrefs)
		if (Time.timeSinceLevelLoad < fadeInTime) {
			float alphaChange = Time.deltaTime / fadeInTime;
			currentColor.a += alphaChange;
			Debug.Log(this + " Delta: " + alphaChange + " for Alpha result: " + currentColor.a);
			fadePanel.color = currentColor;
		} else {
			// TODO fire Particle system
		}

	}
	*/
}
